import { Link } from "react-router-dom";

const Logo = () => {
    return (
        <div>
            <Link><h1 className="text-xl font-semibold">Logo</h1></Link>
        </div>
    );
};

export default Logo;